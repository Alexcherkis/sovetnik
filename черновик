Дата: 10 июня 2026 г.

Проведен глубокий технический аудит и оптимизация SEO сайта («Советникъ — Экспертное Бюро»):

1. Исправлена критическая ошибка региональных страниц (PSEO):
   В файле `src/data/citySpecifics.ts` исправлена функция `getCitySpecificContent`. Ранее из-за разницы в символах (дефисы в URL vs нижние подчеркивания в ключах базы данных) страницы крупных городов (Нижний Новгород и Ростов-на-Дону) загружались пустыми. Теперь они корректно подгружают уникальные описания, суды и местные FAQ, что исключает признание страниц дубликатами поисковиками и поднимает позиции в этих регионах.

2. Настройка robots.txt и параметров индексации:
   В шаблоне `public/robots.txt` и скрипте генерации в `vite.config.ts` для сайтов-клонов удален протокол `https://` из директивы `Host`. Директива приведена к строгому стандарту (чистый домен: `buro-sovetnik.com` и `sovetnik-cno.ru`), что предотвращает ошибки сканирования роботами Яндекса.

3. Полная автоматизация и оптимизация Yandex Feed (`yandex-feed.yml`):
   - Скрипт `scripts/generate-yandex-feed.cjs` переписан на динамический парсинг. Он автоматически считывает все 41 услуги из исходного кода (`src/data/constants.tsx`), вытягивает актуальные цены, уникальные описания и сопоставляет их с картинками (без опечаток в расширениях файлов вроде `.png` вместо `.webp`).
   - Устранена критическая битая ссылка (404) для ключевой услуги оценки бизнеса: теперь slug генерируется как `business-valuation-llc` (вместо старого `business-valuation`).
   - Добавлена выгрузка фида сразу в две папки: в исходную `public/` и сборочную `dist/` (что решает проблему отставания файла на одну сборку при FTP-деплое).
   - Каталог выгружаемых в Яндекс Бизнес/Поиск услуг увеличен с 21 до всех 41 услуг, обеспечивая максимальный охват ключевых запросов в выдаче.

Все изменения внесены исключительно во внутреннюю программную логику и конфигурации. Визуальный дизайн, верстка и интерфейс сайта для пользователей никак не пострадали.

4. Интеграция юридических реквизитов (E-E-A-T) и оптимизация локального SEO на PSEO-страницах:
   - В файл `.env` добавлены реальные регистрационные данные ООО «Экспертное Бюро "Советникъ"»: ИНН `0274131774` и ОГРН `1080274006988`.
   - В `src/config/site.ts` обновлен экспорт реквизитов (`COMPANY_INN` и `COMPANY_OGRN`) с безопасными дефолтными значениями, что позволяет поисковикам автоматически сопоставлять сайт с госреестрами для повышения траста.
   - В `src/components/SEO.tsx` настроена условная выгрузка локальной разметки schema.org и мета-тегов: на страницах конкретных городов (PSEO) исключены физический адрес и координаты головного офиса в Уфе во избежание санкций Google за "fake coordinates". При этом сохраняется корректный тег зоны обслуживания (`areaServed`) и название города в `addressLocality`.
   - Реквизиты (legalName, taxID, leiCode) добавлены в общую разметку `ProfessionalService` для подтверждения легитимности организации.
5. Оптимизация производительности (Performance) и доступности (Accessibility) по Google Lighthouse:
   - В [index.html](file:///Users/alex/Downloads/советникъ---экспертное-бюро/index.html) оптимизировано подключение шрифтов Google Fonts. Ссылка заменена на асинхронный паттерн с `rel="preload"`, что устранило предупреждение о ресурсах, блокирующих рендеринг (Render-blocking requests), и должно снизить время первой отрисовки контента (FCP).
   - В компоненте [SpeedDial.tsx](file:///Users/alex/Downloads/советникъ---экспертное-бюро/src/components/SpeedDial.tsx) добавлены явные атрибуты `aria-label` для плавающих кнопок связи (Telegram, WhatsApp, телефон), что решило проблему «Links do not have a discernible name» в тесте доступности.
